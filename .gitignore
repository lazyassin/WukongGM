# build output
/dist/
*.zip

# user files that shouldn't be committed with personal edits
# (the versions in src/ are the shipped defaults)
/src/WukongGM/config.local.txt

# editor / OS
.vscode/
.idea/
*.swp
Thumbs.db
desktop.ini
.DS_Store

# logs pulled in for debugging
*.log
